## Kernel Sim

### Configure

```
# Configure workspace
$ python3 configure.sh -b [BUILD_TYPE]
```

Build type:

- `DEBUG:` default value
- `RELEASE:`

### Building

```
# Building Kernel Sim
$ cd build && make
```
