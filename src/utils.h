#ifndef _UTILS_H
#define _UTILS_H

#define RED 1
#define GREEN 2
#define BLUE 3

#include <stdio.h>

inline void cprint(const char* text, int color)
{
    switch (color)
    {
    case RED:
        break;
    case GREEN:
        break;
    case BLUE:
        break;
    default:
        break;
    }
}
#endif